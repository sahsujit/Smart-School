import axios from 'axios';

const API_URL = 'https://api.example.com/notices';

export const fetchNotices = async () => {
  return [];
};

export const addNotice = async (notice) => {
  console.log('Add notice', notice);
};
